# Git Config

- Scope: cross-platform
- Real target: `~/.gitconfig` and related includes
- Purpose: shared Git identity, aliases, editor, diff, and signing defaults

Keep machine- or employer-specific overrides outside this directory unless they become stable enough to template.
